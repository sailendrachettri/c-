using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PolyMor : MonoBehaviour
{
    // METHOD OVERLOADING
    /*
    void Start()
    {
        // Search();
        // Search(001);
        Search("Sailendra", 001);
    }

    public void Search()
    {
        Debug.Log("search method with empty field.");
    }

    public void Search(int rollNum)
    {
        Debug.Log("Search method with rollNum: " + rollNum);
    }

    public void Search(string Name, int rollNum)
    {
        Debug.Log("Search method with Name " + Name + " and " + " roll Number " + rollNum);
    }
    */


    // METHOD OVERRIDING
    
}
